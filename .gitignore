# ── Python / Backend
venv/
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# Environment
backend/.env
.env
.env.local

# Secrets — Google service account keys (NEVER commit)
sa-key.json
backend/sa-key.json
*-sa-key.json
*.serviceaccount.json
.env.*.local

# Index build checkpoint — not needed at runtime
backend/data/index/embed_checkpoint.json

# Runtime indexes ARE committed (needed for Docker/EC2 deploy):
#   backend/data/index/faiss.index
#   backend/data/index/chunks.pkl
#   backend/data/index/bm25.pkl

# PDFs — ignore source PDFs, but keep the combined one (served at runtime)
backend/data/raw/labor_laws.pdf
backend/data/raw/minimum_wage_law.pdf
backend/data/raw/social_security_law.pdf
backend/data/raw/trade_union_law.pdf
# backend/data/raw/cambodian_labor_laws.pdf  ← committed (served PDF)

# JSONL source data — optional, keep if small enough (~5MB ok)
# backend/law_chunks.jsonl
# backend/law_chunks_v2.jsonl

# Database
*.db
*.sqlite3

# macOS
.DS_Store
**/.DS_Store

# ── Node / Frontend ───────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local
frontend/.env.*.local
# Vite cache
frontend/.vite/

# ── Editor ────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Logs ─────────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── OS ───────────────────────────────────────────────────────────
Thumbs.db